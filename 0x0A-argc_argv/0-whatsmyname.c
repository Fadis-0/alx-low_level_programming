#include <stdio.h>

/** 
 * main - Entry point
 * Description: Prints the name of the program
 * @argc: Argument count
 * @argv: Argument vector
 * Return: 0 (Success)
 */

int main(int argc, char *argv[])
{
 printf("%s\n", argv[0]);
 return (0);
}
